(defun my-web-mode-indent-setup()
  (setq web-mode-markup-indent-offset 4);html tag
  (setq web-mode-css-indent-offset 4);css
  (setq web-mode-code-indent-offset 4));js code

(provide 'change-tablength)
